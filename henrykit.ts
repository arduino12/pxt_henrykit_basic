/**
 * Range comparison options.
 */
const enum InOut {
	//% block="got in"
	GotIn,
	//% block="got out of"
	GotOut,
	//% block="in"
	In,
	//% block="out of"
	Out,
}

/**
 * Probe types: capacitive(black PCB), resistive(red PCB).
 */
const enum ProbeType {
    //% block="capacitive"
    Capacitive,
    //% block="resistive"
    Resistive,
}
