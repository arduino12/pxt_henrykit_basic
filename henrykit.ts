/**
 * Range comparison options.
 */
const enum InOut {
	//% block="out of"
	Out,
	//% block="in"
	In,
	//% block="got out of"
	GotOut,
	//% block="got in"
	GotIn,
}

/**
 * Probe types: capacitive(black PCB), resistive(red PCB).
 */
const enum ProbeType {
	//% block="capacitive"
	Capacitive,
	//% block="resistive"
	Resistive,
}
